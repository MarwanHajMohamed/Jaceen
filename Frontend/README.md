## Tech Stack

Jaceen is built on React + TypeScript + Vite on the frontend, Express.js on the backend, Node.js as the server side runtime, and MongoDB for the database (MERN Stack).

## How to run

To run the server:

1. `cd Backend`
2. `npm start`

To run the frontend:

1. `cd Frontend`
2. `npm run dev`
